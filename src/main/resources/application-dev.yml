server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysqlErp:3306/erp?useSSL=false&allowPublicKeyRetrieval=true
    username: ${datasource.dev.usernames}
    password: ${datasource.dev.password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
      ddl-auto: update
    properties:
      show-sql: true
      format_sql: true

